import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#2b3d4f'
    },
    secondary: {
      main: '#bdc3c7'
    }
  }
})

export default theme;